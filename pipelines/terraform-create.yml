format_version: 10
pipelines:
  Expense-tf-apply:
    group: Terraform
    materials:
      git-b844969:
        git: https://github.com/DevOps-iteration2/expense-terraform.git
        shallow_clone: false
        auto_update: true
        branch: main
    secure_variables:
      TF_VAR_vault_token: AES:OzbVV1ZXJb+JVCtzrJzzFg==:ZsEIrB7Aiky37CxKCSByJ2B66t86NP461x3pH4fsx+xwm/wPfsevoCBFHPVlDoCS
    stages:
      - DEV:
          fetch_materials: true
          environment_variables:
            env : dev
          approval:
            type: manual
          jobs:
            terraform-apply:
              tasks:
                - exec: {command: "terraform", arguments: [ "init", "-backend-config=env-dev/state.tfvars" ], run_if: passed}
                - exec: {command: "terraform", arguments: [ "apply", "-var-file=env-dev/main.tfvars", "-auto-approve" ], run_if: passed}